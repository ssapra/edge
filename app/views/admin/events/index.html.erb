<div id="headerwrap">
    <div class="container">
    <div class="row">
      <div class="col-lg-8 col-lg-offset-2">
        <% @events.each do |event| %>
          <h1><%= link_to event.title, admin_event_path(id: event.id) %></h1>
        <% end %>

        <%= link_to "New Event", new_admin_event_path %>

      </div>
    </div><!-- /row -->
  </div> <!-- /container -->
</div><!-- /headerwrap -->
